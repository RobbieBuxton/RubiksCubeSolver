#ifndef __PRINT_STATE_H__
#define __PRINT_STATE_H__

#include "insttypes.h"

/**
 * Displays the current state of the registers and memory.
 *
 * @param state Current machine state
 */
void print_state(State *state);

#endif  // __PRINT_STATE_H__

